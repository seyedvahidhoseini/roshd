# چت بات مهارت‌های فریلنسرها - مستندات فارسی

## 📋 معرفی پروژه

این پروژه یک اپلیکیشن وب فرانت‌اند است که امکان مدیریت و چت با فریلنسرها بر اساس مهارت‌هایشان را فراهم می‌کند. کاربران می‌توانند فریلنسرهای جدید اضافه کنند و با آن‌ها در مورد مهارت‌هایشان چت کنند.

## 🚀 ویژگی‌های اصلی

### 1. افزودن فریلنسر جدید
- **آپلود فایل PDF**: کاربران می‌توانند فایل PDF مهارت‌های فریلنسر را آپلود کنند
- **اطلاعات پایه**: امکان وارد کردن نام و توضیحات فریلنسر
- **اعتبارسنجی ورودی**: بررسی وجود نام و فایل قبل از ارسال
- **پردازش خودکار**: سرور به صورت خودکار مهارت‌ها را از PDF استخراج و ایندکس می‌کند
- **انتخاب خودکار**: پس از ایجاد، فریلنسر جدید به صورت خودکار انتخاب می‌شود
- **پاک‌سازی فرم**: فرم پس از ایجاد موفقیت‌آمیز پاک می‌شود

### 2. مدیریت فریلنسرها
- **لیست فریلنسرها**: نمایش تمام فریلنسرهای موجود
- **انتخاب فریلنسر**: امکان انتخاب فریلنسر برای چت
- **ذخیره انتخاب**: انتخاب فریلنسر در localStorage ذخیره می‌شود
- **به‌روزرسانی**: دکمه به‌روزرسانی لیست فریلنسرها
- **نمایش اطلاعات**: نمایش نام و توضیحات هر فریلنسر
- **حالت انتخاب**: نمایش بصری فریلنسر انتخاب شده

### 3. سیستم چت
- **چت هوشمند**: امکان چت با فریلنسر انتخاب شده در مورد مهارت‌هایش
- **رابط کاربری زیبا**: طراحی مدرن و ریسپانسیو
- **اسکرول خودکار**: پیام‌ها به صورت خودکار اسکرول می‌شوند
- **ارسال با Enter**: امکان ارسال پیام با کلید Enter (Shift+Enter برای خط جدید)
- **نمایش وضعیت**: نمایش وضعیت ارسال پیام
- **مدیریت خطا**: نمایش خطاها در صورت بروز مشکل
- **چت جدید**: شروع چت جدید با انتخاب فریلنسر جدید

### 4. رابط کاربری
- **طراحی ریسپانسیو**: سازگار با تمام اندازه‌های صفحه
- **راست‌چین**: پشتیبانی کامل از زبان فارسی
- **رنگ‌بندی مدرن**: استفاده از Tailwind CSS
- **تجربه کاربری بهینه**: انیمیشن‌ها و ترانزیشن‌های نرم
- **حالت‌های تعاملی**: hover effects و focus states
- **نمایش وضعیت**: نمایش loading states و disabled states

### 5. مدیریت حالت و ذخیره‌سازی
- **localStorage**: ذخیره انتخاب فریلنسر
- **State Management**: مدیریت حالت‌های مختلف اپلیکیشن
- **Auto-save**: ذخیره خودکار تنظیمات کاربر
- **Session Persistence**: حفظ انتخاب در طول جلسه

### 6. مدیریت خطا و اعتبارسنجی
- **اعتبارسنجی فرم**: بررسی ورودی‌های کاربر
- **مدیریت خطاهای API**: نمایش پیام‌های خطای مناسب
- **Try-Catch Blocks**: مدیریت خطاها در تمام عملیات async
- **User Feedback**: نمایش پیام‌های مناسب به کاربر

## 🛠️ تکنولوژی‌های استفاده شده

### Frontend
- **React 19.1.1**: کتابخانه اصلی رابط کاربری
- **Vite 7.1.7**: ابزار ساخت و توسعه
- **Tailwind CSS 4.1.14**: فریمورک CSS
- **ESLint**: ابزار بررسی کیفیت کد

### Backend Integration
- **REST API**: ارتباط با سرور از طریق API
- **FormData**: آپلود فایل‌های PDF
- **localStorage**: ذخیره تنظیمات کاربر

## 📁 ساختار پروژه

```
skill-bot-frontend/
├── public/                 # فایل‌های استاتیک
├── src/
│   ├── App.jsx            # کامپوننت اصلی
│   ├── App.css            # استایل‌های اصلی
│   ├── main.jsx           # نقطه ورود اپلیکیشن
│   ├── index.css          # استایل‌های عمومی
│   └── assets/            # تصاویر و فایل‌های رسانه
├── package.json           # وابستگی‌ها و اسکریپت‌ها
├── vite.config.js         # تنظیمات Vite
├── tailwind.config.js     # تنظیمات Tailwind
└── eslint.config.js       # تنظیمات ESLint
```

## 🔧 نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (نسخه 18 یا بالاتر)
- npm یا yarn

### مراحل نصب

1. **کلون کردن پروژه**
```bash
git clone <repository-url>
cd skill-bot-frontend
```

2. **نصب وابستگی‌ها**
```bash
npm install
```

3. **تنظیم متغیر محیطی**
```bash
# ایجاد فایل .env
echo "VITE_API_BASE=http://localhost:8000" > .env
```

4. **اجرای پروژه**
```bash
npm run dev
```

5. **باز کردن در مرورگر**
```
http://localhost:5173
```

## 📖 راهنمای استفاده

### افزودن فریلنسر جدید

1. **وارد کردن اطلاعات**:
   - نام فریلنسر را در فیلد مربوطه وارد کنید
   - توضیحات اختیاری را اضافه کنید

2. **آپلود فایل PDF**:
   - روی ناحیه "برای انتخاب فایل کلیک کنید" کلیک کنید
   - فایل PDF مهارت‌های فریلنسر را انتخاب کنید

3. **ایجاد فریلنسر**:
   - روی دکمه "ایجاد فریلنسر" کلیک کنید
   - منتظر پردازش فایل توسط سرور باشید

### چت با فریلنسر

1. **انتخاب فریلنسر**:
   - از لیست فریلنسرها یکی را انتخاب کنید
   - یا فریلنسر جدیدی ایجاد کنید

2. **شروع چت**:
   - در قسمت چت پیام خود را تایپ کنید
   - Enter را فشار دهید یا روی "ارسال" کلیک کنید

3. **سوالات پیشنهادی**:
   - "مهارت‌های شما چیست؟"
   - "در چه زمینه‌ای تخصص دارید؟"
   - "نمونه کارهای شما کدامند؟"

## 🔌 API Endpoints

### دریافت لیست فریلنسرها
```http
GET /freelancers
```

### ایجاد فریلنسر جدید
```http
POST /freelancers
Content-Type: multipart/form-data

{
  "name": "نام فریلنسر",
  "description": "توضیحات",
  "file": "فایل PDF"
}
```

### ارسال پیام چت
```http
POST /chat
Content-Type: application/json

{
  "freelancer_id": "شناسه فریلنسر",
  "query": "سوال کاربر"
}
```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها
فایل `tailwind.config.js` را ویرایش کنید:

```javascript
export default {
  content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      colors: {
        primary: '#your-color',
        secondary: '#your-color'
      }
    }
  },
  plugins: []
}
```

### تغییر فونت
در فایل `index.css`:

```css
@import url('https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700&display=swap');

body {
  font-family: 'Vazirmatn', sans-serif;
}
```

## 🚀 Build و Deploy

### Build برای تولید
```bash
npm run build
```

### Preview نسخه Build شده
```bash
npm run preview
```

### Deploy
فایل‌های موجود در پوشه `dist` را روی سرور وب خود آپلود کنید.

## 🔧 تنظیمات پیشرفته

### متغیرهای محیطی
```bash
# فایل .env
VITE_API_BASE=http://localhost:8000  # آدرس سرور بک‌اند
```

### تنظیمات Vite
```javascript
// vite.config.js
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true
  }
})
```

### تنظیمات Tailwind
```javascript
// tailwind.config.js
export default {
  content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      fontFamily: {
        'vazir': ['Vazirmatn', 'sans-serif']
      }
    }
  }
}
```

## 📱 ویژگی‌های موبایل

- **طراحی ریسپانسیو**: سازگار با تمام اندازه‌های صفحه
- **Touch-friendly**: دکمه‌ها و عناصر تعاملی مناسب برای لمس
- **Mobile-first**: طراحی اولویت‌دار موبایل
- **Viewport optimization**: بهینه‌سازی برای نمایش در موبایل

## 🎯 بهترین شیوه‌ها

### کدنویسی
- استفاده از React Hooks برای مدیریت state
- اعتبارسنجی ورودی‌ها قبل از ارسال
- مدیریت خطاها با try-catch
- استفاده از async/await برای عملیات غیرهمزمان

### UX/UI
- نمایش loading states
- پیام‌های خطای واضح
- طراحی intuitive
- پشتیبانی از keyboard shortcuts

### Performance
- Lazy loading برای کامپوننت‌های بزرگ
- Memoization برای بهینه‌سازی re-renders
- بهینه‌سازی bundle size
- استفاده از React.memo در صورت نیاز

## 🐛 عیب‌یابی

### مشکلات رایج

1. **خطای اتصال به سرور**:
   - بررسی کنید سرور بک‌اند در حال اجرا باشد
   - آدرس API را در فایل `.env` بررسی کنید
   - CORS policy را بررسی کنید

2. **آپلود فایل ناموفق**:
   - اطمینان حاصل کنید فایل PDF است
   - اندازه فایل را بررسی کنید (معمولاً محدودیت 10MB)
   - فرمت فایل را بررسی کنید

3. **مشکل در نمایش فارسی**:
   - اطمینان حاصل کنید `dir="rtl"` تنظیم شده
   - فونت فارسی نصب شده باشد
   - encoding صفحه را بررسی کنید

4. **مشکل در localStorage**:
   - بررسی کنید localStorage فعال باشد
   - در حالت incognito ممکن است محدود باشد
   - داده‌های قدیمی را پاک کنید

5. **مشکل در چت**:
   - اطمینان حاصل کنید فریلنسر انتخاب شده
   - اتصال اینترنت را بررسی کنید
   - console را برای خطاها بررسی کنید

## 📝 اسکریپت‌های موجود

```json
{
  "dev": "vite",           // اجرای سرور توسعه
  "build": "vite build",   // ساخت نسخه تولید
  "lint": "eslint .",      // بررسی کیفیت کد
  "preview": "vite preview" // پیش‌نمایش نسخه تولید
}
```

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات خود را commit کنید
4. Pull Request ارسال کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 📞 پشتیبانی

برای پشتیبانی و گزارش باگ، لطفاً issue جدیدی در repository ایجاد کنید.

## 🔮 ویژگی‌های آینده (پیشنهادی)

### بهبودهای ممکن
- **جستجو در فریلنسرها**: امکان جستجو بر اساس نام یا مهارت
- **دسته‌بندی مهارت‌ها**: گروه‌بندی فریلنسرها بر اساس تخصص
- **امتیازدهی**: سیستم امتیازدهی برای فریلنسرها
- **تاریخچه چت**: ذخیره و بازیابی تاریخچه مکالمات
- **صادرات چت**: امکان صادرات مکالمات به PDF
- **نوتیفیکیشن**: اعلان‌های real-time
- **چت گروهی**: امکان چت با چندین فریلنسر
- **پروفایل کاربری**: مدیریت پروفایل کاربر

### بهبودهای فنی
- **TypeScript**: اضافه کردن type safety
- **State Management**: استفاده از Redux یا Zustand
- **Testing**: اضافه کردن unit tests و integration tests
- **PWA**: تبدیل به Progressive Web App
- **Offline Support**: پشتیبانی از حالت آفلاین
- **Dark Mode**: اضافه کردن حالت تاریک

## 📊 آمار پروژه

- **تعداد کامپوننت‌ها**: 1 کامپوننت اصلی
- **تعداد API endpoints**: 3 endpoint
- **حجم bundle**: ~50KB (gzipped)
- **زمان load**: <2 ثانیه
- **پشتیبانی مرورگر**: Chrome, Firefox, Safari, Edge

## 🏗️ معماری پروژه

```
Frontend (React)
├── App.jsx (کامپوننت اصلی)
├── State Management (useState, useEffect)
├── API Integration (fetch)
├── Local Storage (localStorage)
└── UI Components (Tailwind CSS)

Backend Communication
├── GET /freelancers (دریافت لیست)
├── POST /freelancers (ایجاد فریلنسر)
└── POST /chat (ارسال پیام)
```

## 🔒 امنیت

### ملاحظات امنیتی
- **اعتبارسنجی ورودی**: بررسی تمام ورودی‌های کاربر
- **Sanitization**: پاک‌سازی داده‌های ورودی
- **CORS**: تنظیم صحیح CORS policy
- **File Upload**: محدودیت نوع و اندازه فایل
- **Error Handling**: عدم افشای اطلاعات حساس در خطاها

---

**نکته**: این مستندات بر اساس کد فعلی پروژه تهیه شده است. در صورت تغییرات در کد، لطفاً مستندات را به‌روزرسانی کنید.

**تاریخ آخرین به‌روزرسانی**: $(date)
